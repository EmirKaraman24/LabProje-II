<Window x:Class="SocialNetworkAnalysis.UI.EdgeDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Kenar Ekle" Height="200" Width="300"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Kaynak Düğüm:" Margin="0,0,0,5"/>
        <ComboBox x:Name="CmbSource" Grid.Row="1" Margin="0,0,0,10" IsEditable="True"/>

        <TextBlock Grid.Row="2" Text="Hedef Düğüm:" Margin="0,0,0,5"/>
        <ComboBox x:Name="CmbTarget" Grid.Row="3" Margin="0,0,0,15" IsEditable="True"/>

        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Tamam" Width="80" Height="30" Margin="0,0,10,0" Click="BtnOk_Click" IsDefault="True"/>
            <Button Content="İptal" Width="80" Height="30" Click="BtnCancel_Click" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>

